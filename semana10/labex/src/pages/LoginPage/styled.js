import styled from "styled-components";

export const StyledForm = styled.form`
    width: 50vw;
    min-width: 320px;
    margin: 15px;

    * {
        width: 100%;
    }

    input, select {
        margin: .3rem 0;
        height: 2rem;
        padding: .5rem;
    }

    textarea {
        margin: .3rem 0;
        padding: .5rem;
    }

    button {
        margin: 1rem 0;
    }

`